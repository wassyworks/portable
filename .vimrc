" 行数表示
set number

" シェル
set sh=zsh

" カレントディレクトリを自動で移動する
set autochdir

" tab
set tabstop=4
set shiftwidth=4
set list
set listchars=tab:»\ ,extends:>

" エンコーディング
set fileencoding=utf-8
set encoding=utf-8

" バックアップファイルを出力しない
set nobackup

" undoファイルを出力しない
set noundofile

" Leaderキーの割当
let mapleader="\<Space>"

